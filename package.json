{
  "name": "x-neutrino-basic",
  "version": "1.0.0",
  "description": "- Create a repository on GitHub (https://github.com/new) and clone it locally - `npm init` (will create the package.json) - When asked use 'src/index.js' as entry point - Install the neutrino dev dependencies: `npm i -D neutrino neutrino-preset-web` - Inside your project folder, create a folder called `src` with a `index.js` inside it - Edit the `package.json` file and add the `start` and `build` scripts like   ```   \"build\": \"neutrino build --use neutrino-preset-web\",   \"start\": \"neutrino start --use neutrino-preset-web\",   ``` - Run the server with `npm run start` (or actually just `npm start`) - Check that everything went smoothly in your browser - Stop the server (`CTRL+C`) - Install jQuery with `npm i jquery` (no `-D` because it is not a development dependency) - In your `src/index.js` import jQuery like:   `import $ from 'jquery';` - Restart the server (you know how to do it) - Add something like:   ```js   $(() => {     $('body').append('<h1>Hello World!</h1>');   });   ```   and look at your browser if the text appears in the body - Create a `styles.css` (inside the `src` folder) with body background-color whatever - Add your CSS file by using `import './styles.css';` - Look at your browser ;) (fancy.. hueeee)",
  "main": "src/index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "neutrino build",
    "start": "neutrino start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zeropaper/x-neutrino-basic.git"
  },
  "keywords": [
    "neutrino",
    "bootstrap",
    "jquery",
    "sass",
    "exercise"
  ],
  "author": "Valentin Vago <zeropaper@irata.ch>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zeropaper/x-neutrino-basic/issues"
  },
  "homepage": "https://github.com/zeropaper/x-neutrino-basic#readme",
  "devDependencies": {
    "@neutrinojs/airbnb-base": "^8.0.10",
    "@neutrinojs/web": "^8.0.10",
    "neutrino": "^8.0.10",
    "node-sass": "^4.7.2",
    "sass-loader": "^6.0.6"
  },
  "dependencies": {
    "bootstrap": "^4.0.0-beta.2",
    "jquery": "^3.2.1",
    "popper.js": "^1.12.9"
  }
}
